# Copy to .env.local for local dev (already gitignored by Vite's default .gitignore).
# Points the frontend at the FastAPI backend. Defaults to localhost:8000 if unset.
VITE_API_BASE=http://127.0.0.1:8000
