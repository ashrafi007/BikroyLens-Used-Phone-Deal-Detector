# Dependencies for the cloud pipeline: scraper, loader, normalizer, model
# retrain, and the pipeline_service trigger app that runs them (see
# pipeline_service/main.py). The main API has its own separate, lighter
# requirements.txt (api/requirements.txt) -- it doesn't need Scrapy,
# xgboost, scikit-learn, or any of this.
Scrapy==2.18.0
psycopg2-binary==2.9.12
pandas==3.0.5
numpy==2.5.2
scikit-learn==1.9.1
xgboost==3.4.1
python-dotenv==1.2.3
fastapi==0.141.1
uvicorn==0.53.0
